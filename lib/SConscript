# -*- python -*-
Import('env')

env.SharedLibrary('citizen', map(lambda str: "../src/" + str,
				 Split("Citizen.cc Trace.cc MaskExceptions.cc")))
env.SharedLibraryIncomplete('fitsio', map(lambda str: "../src/" + str,
					  Split("DiskImageResourceFITS.cc")),
                            LIBS=["cfitsio"])
