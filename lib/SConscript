# -*- python -*-
Import('env')

env.SharedLibrary('fw', map(lambda str: "../src/" + str,
                            ["Citizen.cc",
                             "DataProperty.cc",
                             "Demangle.cc",
                             "Trace.cc",
                             ]))
env.SharedLibraryIncomplete('fitsio', map(lambda str: "../src/" + str,
					  Split("DiskImageResourceFITS.cc")),
                            LIBS=["cfitsio"])
