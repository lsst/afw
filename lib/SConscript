# -*- python -*-
Import('env')

env.SharedLibrary(
	'fw',
	["../src/%s" % (src,) for src in (
		"DiskImageResourceFITS.cc",
		"Exposure.cc",
		"Kernel/Kernel.cc",
		"Kernel/FixedKernel.cc",
		"Kernel/AnalyticKernel.cc",
		"Kernel/LinearCombinationKernel.cc",
		"WCS.cc",
	)],
	LIBS=env.getlibs("boost mwi vw wcslib cfitsio")
)
