setupRequired(daf_base)
setupRequired(daf_data)
setupRequired(daf_persistence)
setupRequired(pex_exceptions)
setupRequired(pex_logging)
setupRequired(pex_policy)
setupRequired(swig >= 1.3.34)
setupRequired(vw >= 1.0.1+2)
setupRequired(cfitsio)
setupRequired(wcslib)
setupRequired(xpa)
setupRequired(numpy)
setupRequired(minuit)

setupOptional(afwdata)

envPrepend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envPrepend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)

envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
