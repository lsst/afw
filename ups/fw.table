setupRequired(mwi)
setupRequired(vw)
setupRequired(cfitsio)
setupRequired(wcslib)
setupRequired(xpa)

setupOptional("numarray")
setupOptional("pyfits")

envAppend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envAppend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)

envAppend(PYTHONPATH, ${PRODUCT_DIR}/python)
